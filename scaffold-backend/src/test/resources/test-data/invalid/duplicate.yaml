dsl: "0.1"
version: "1.0.0"
name: YAML Test Case
namespace: test-yaml
title: Duplicate definition - same namespace/name/version as test-yaml-definition.yaml
spec:
  capabilities:
    - name: dummy
      inputSchema: "{}"
      outputSchema: "{}"
  workers:
    - name: dummy-worker
      capabilities:
        - dummy
      do:
        - setResult:
            set:
              done: true
