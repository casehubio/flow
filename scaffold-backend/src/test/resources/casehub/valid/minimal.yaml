dsl: "0.1"
version: "1.0.0"
name: Minimal Test
namespace: test-valid
spec:
  capabilities:
    - name: doSomething
      inputSchema: "{}"
      outputSchema: "{}"
  workers:
    - name: simple-worker
      capabilities:
        - doSomething
      do:
        - setResult:
            set:
              done: true
